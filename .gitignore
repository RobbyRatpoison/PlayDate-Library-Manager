# --- Personal & Sensitive Data ---
config.json

# --- Local Application State ---
state.json
release_date_migration.json
santa_gifts.json
emulators.json
*_cache.json
group_sources_*.json
steamgifts_wins_*.json
pagywosg_scan_decisions.json

# --- Claude Code ---
.claude/
memory/

# --- Databases ---
*.db
*.db-wal
*.db-shm
*.csv

# --- Logs ---
*.log

# --- Media Assets ---
/static/img/library/
/static/img/backgrounds/*
!/static/img/backgrounds/playdate_default_background.jpg
/static/img/badges/

# --- User Theme ---
theme.json

# --- Python Environment ---
__pycache__/
*.pyc
venv/
.venv/
.env

# --- PyInstaller Build Artifacts ---
dist/
build/
*.spec.bak

# --- Inno Setup Output ---
installer/

# --- Flatpak Build Artifacts ---
flatpak/.flatpak-builder/
flatpak/build-dir/

# --- Generated at Install Time ---
playdate-launch.sh
playdate-launch.bat
.pip-constraints.txt

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Distribution Files ---
git-push.sh
make-zip.sh
CLAUDE.md
*.zip

# --- Plugins ---
# No plugin ships as source in this repo anymore -- every one (the
# first-party "official" catalog plugins, the still-unfinished beta ones,
# and any third-party plugin installed via zip/GitHub) lives in the writable
# plugins dir instead (plugins._user_plugins_dir(), which for a source
# checkout resolves to this same physical plugins/ folder -- BASE_DIR is the
# project root here). Only plugins/__init__.py itself is ever tracked, so a
# blanket subdirectory ignore covers every current AND future plugin id
# without needing a new entry each time one's installed for local testing.
plugins/*/

# --- Personal Reference ---
HOWTO.md
TASKS.md
PLUGINS_STATUS.md
release.py
.aider*

# --- Local Debug Tools ---
# Ad-hoc scripts for live-testing specific bugs against a running dev
# server (e.g. Playwright harnesses) -- kept locally for reuse, not part
# of the shipped app or meant to be reviewed/shipped as source.
tools/debug/
